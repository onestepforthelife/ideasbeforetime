{
  "name": "kiro-tools",
  "version": "1.0.0",
  "description": "Essential tools for Kiro AI Assistant",
  "scripts": {
    "install-all": "npm install && pip install chromadb",
    "test": "node playwright-live-site-test.js",
    "search": "python chromadb-learning-search.py",
    "cache-status": "node -e \"const SessionCache = require('./redis-session-cache.js'); new SessionCache().getSessionContext().then(console.log)\""
  },
  "dependencies": {
    "@sentry/browser": "^7.91.0",
    "@sentry/node": "^7.91.0",
    "redis": "^4.6.0"
  },
  "devDependencies": {
    "@playwright/test": "^1.40.0"
  }
}
